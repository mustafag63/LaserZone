# Server
PORT=5001

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=laserzone
# Uncomment below ONLY on macOS with Homebrew MySQL:
# DB_SOCKET=/tmp/mysql.sock

# JWT
JWT_SECRET=your_super_secret_jwt_key_here_change_this
JWT_EXPIRES_IN=7d
